<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F8F8">

    <!-- Toolbar flush to top, left, right -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#1B2A6D"
        app:title="Create Folder"
        app:titleTextColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Folder Name Input with side padding -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/folderNameLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:boxBackgroundMode="outline"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/folderNameEditText"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Enter folder name"
            android:inputType="text"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Folder Description Input (Optional) with side padding -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/folderDescLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/folderNameLayout"
        app:boxBackgroundMode="outline"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/folderDescEditText"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Enter folder description (optional)"
            android:inputType="text"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Save Folder Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/saveFolderButton"
        android:layout_width="383dp"
        android:layout_height="53dp"
        android:layout_marginTop="32dp"
        android:text="Save Folder"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:backgroundTint="#1B2A6D"
        app:cornerRadius="28dp"
        app:icon="@android:drawable/ic_menu_save"
        app:iconPadding="12dp"
        app:iconTint="@android:color/white"
        app:layout_constraintEnd_toEndOf="@id/folderNameLayout"
        app:layout_constraintStart_toStartOf="@id/folderNameLayout"
        app:layout_constraintTop_toBottomOf="@id/folderDescLayout" />
</androidx.constraintlayout.widget.ConstraintLayout>
